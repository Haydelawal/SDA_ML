<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/more"
        android:title=""
        android:icon="@drawable/ic_baseline_more_vert_24"
        app:showAsAction="always"
        >

        <menu>
            <item android:id="@+id/clear_menu"
                android:title="Clear Input Fiels"
                app:showAsAction="never"
                android:icon="@drawable/ic_cancel"
                />

            <item android:id="@+id/saved_data"
                android:title="Previous Results"
                app:showAsAction="never"
                android:icon="@drawable/ic_data"
                android:iconTintMode="screen"
                android:iconTint="@color/purple_500"
                />

        </menu>


    </item>


</menu>